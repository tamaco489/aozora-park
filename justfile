default:
    @just --list


# ==========================================================================================
# docker compose: エミュレータの起動と停止
# ==========================================================================================
# エミュレータを起動する、healthy になるまで待つ
up:
    docker compose up -d --wait

# エミュレータを停止する
down:
    docker compose down

# イメージを組み立て直す
build:
    docker compose build


# ==========================================================================================
# docker compose: 状態の確認
# ==========================================================================================
# ログを追う
logs:
    docker compose logs -f

# 起動状態と健全性を見る
ps:
    docker compose ps
